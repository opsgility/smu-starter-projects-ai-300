# Documentation only — every package here is preinstalled in the SkillMeUp python-ai container.
# Do NOT run `pip install -r requirements.txt` inside the lab. If a package is missing, that is a
# container bug — file it against vscode/docker/python-ai/Dockerfile, do not add an inline install step.

# Foundry + Azure AI SDKs
azure-ai-ml==1.34.1
azure-ai-projects>=1.0.0
azure-ai-evaluation==1.18.3
agent-framework>=1.0.0
azure-identity>=1.17.0
azure-search-documents>=11.5.1
azure-storage-blob>=12.20.0
azure-keyvault-secrets>=4.8.0

# Observability
azure-monitor-opentelemetry>=1.6.0
opentelemetry-instrumentation-fastapi>=0.48b0

# Dispatcher API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
httpx>=0.27.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# Evaluation + testing
pytest>=8.0.0
pyyaml>=6.0.0
