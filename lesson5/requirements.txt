# Documentation only — every package below is already installed in the
# python-ai container image. Do NOT `pip install -r requirements.txt`.
azure-ai-ml==1.34.1
azure-identity
mlflow<=2.16.2
azureml-mlflow
xgboost<=2.1.1
scikit-learn
pandas
pyarrow
numpy
